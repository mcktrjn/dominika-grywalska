import { Container, Section, Typography } from "../../components";

export const Hero = () => {
  return (
    <Section>
      <Container>
        <div style={{ paddingTop: "100vh" }} />
        <Typography tag="p">Hero</Typography>
      </Container>
    </Section>
  );
};
